{
  "name": "garbage-disintegrator",
  "version": "0.0.2",
  "description": "scan text file or string with Regex, generate a file without repeated regex's string.",
  "bin": {
    "rr": "bin/remove-repeated-string"
  },
  "scripts": {
    "test": "jest",
    "testWithCoverage": "jest --coverage",
    "build": "rm -rf lib && tsc src/*.ts src/**/* --outDir lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rockerway/garbage-disintegrator.git"
  },
  "author": "tod.shen",
  "bugs": {
    "url": "https://github.com/rockerway/garbage-disintegrator/issues"
  },
  "homepage": "https://github.com/rockerway/garbage-disintegrator#readme",
  "dependencies": {
    "commander": "^2.9.0",
    "chalk": "^2.4.2"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "jest": "^24.7.1",
    "prettier": "^1.16.4",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.3"
  }
}
